'use strict';

const definitions = {
  Error: require('./Error'),
  Errors: require('./Errors'),

  Task: require('./Task'),
  Tasks: require('./Tasks'),
  NewTask: require('./NewTask'),
  UpdateTask: require('./UpdateTask')
};

module.exports = definitions;
